class GraphData:
    algorithm = None    # The search algorithm being used
    fig = None          # The matplotlib figure for the graph visualization
    ax = None           # The matplotlib axis for the graph visualization
    G = None            # The graph data structure
    pos = None          # Positions for every node in the graph (fixed)
